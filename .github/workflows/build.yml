name: Java CI with Maven

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      # -U zorla günceller, -DskipTests testleri atlayarak hızı artırır
      run: mvn clean package -U -DskipTests

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: HT-Homes-Plus-Jar
        path: target/*.jar
        name: Java CI with Maven

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build with Maven
      run: mvn clean package -B

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: HTHomes-Release
        path: target/*.jar
